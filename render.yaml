services:
  - type: web
    name: svg-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python SVGenerate.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
